## 电机测试
通过按键1实现点动，按键1按下时电机启动，松开停止，再次按下改变方向（前进/后退）。

## 屏幕测试
SPI通信协议硬件实现。
通过按键2切换颜色，实现全屏填充三原色（红绿蓝）、白色、黑色。

## 音频测试
I2S + DMA双缓冲。
播放一段音频，hello.wav转换成audio_data.h数组形式，去掉WavHeader，仅保留PCM数据。
音频采样率44100Hz，量化位数16bit，单声道。

## 传感器测试
ADC + UART
